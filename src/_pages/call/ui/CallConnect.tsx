import { FC } from 'react';

interface CallConnectProps {
  meetingId: string;
  meetingName: string;
  userId: string;
  userName: string;
}

export const CallConnect: FC<CallConnectProps> = () => {
  return <div>CallConnect</div>;
};
